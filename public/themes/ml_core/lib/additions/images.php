<?php

/**
 * Add featured image theme support.
 */
add_theme_support( 'post-thumbnails' );

/**
 * Add image sizes
 */

add_image_size( 'flexible-content-image', 600, 600);
add_image_size( 'flex-box', 580, 580);
add_image_size( 'archive-image', 520, 360);
add_image_size( 'archive-image-top', 520, 400);
add_image_size( 'archive-image-small', 380, 280);
add_image_size( 'featured-image', 1600, 900);
add_image_size( 'related-image', 380, 260);
add_image_size( 'home-image', 520, 480);
add_image_size( 'clinic-main-image', 1200, 600);
add_image_size( 'clinic-doctor', 400, 400);
add_image_size( 'clinic-staff', 160, 160);
